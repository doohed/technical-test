import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#66bb6a',
      light: "#81c784",
      dark: "#388e3c",
      contrastText: '#fff',
    },
  },
});

export default theme;
